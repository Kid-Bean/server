spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/kidbean
    username: sa
    password: test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: false

logging.level:
  org.hibernate.SQL: debug